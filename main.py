def get_personal_color():
    print("\n--- 🎨 퍼스널 컬러 자가 진단 ---")
    print("질문에 답변하여 본인의 유형을 찾아보세요.")
    
    # 간단한 로직을 위한 점수 합산 (Warm vs Cool)
    warm = 0
    cool = 0
    
    q1 = input("1. 피부톤이 대체로 어떤가요? (A. 노란기/따뜻함, B. 붉은기/차가움): ").upper()
    q2 = input("2. 잘 어울리는 액세서리는? (A. 골드, B. 실버): ").upper()
    q3 = input("3. 햇볕에 타면 피부가 어떻게 되나요? (A. 검게 탄다, B. 빨갛게 익는다): ").upper()

    if q1 == 'A': warm += 1
    else: cool += 1
    if q2 == 'A': warm += 1
    else: cool += 1
    if q3 == 'A': warm += 1
    else: cool += 1

    # 세부 계절 판정 (단순화)
    if warm > cool:
        bright = input("4. 이미지가 어떤가요? (A. 밝고 생기있음, B. 차분하고 깊이있음): ").upper()
        return "봄 웜톤" if bright == 'A' else "가을 웜톤"
    else:
        bright = input("4. 이미지가 어떤가요? (A. 맑고 깨끗함, B. 강렬하고 선명함): ").upper()
        return "여름 쿨톤" if bright == 'A' else "겨울 쿨톤"

def get_outfit_recommendation(color_type, temp):
    # 퍼스널 컬러별 추천 색상
    color_palettes = {
        "봄 웜톤": "복숭아색, 코랄, 옐로우 그린, 브라이트 베이지",
        "여름 쿨톤": "라벤더, 스카이 블루, 파스텔 핑크, 소프트 화이트",
        "가을 웜톤": "머스타드, 테라코타, 카키, 딥 브라운",
        "겨울 쿨톤": "로열 블루, 버건디, 블랙, 선명한 화이트"
    }

    # 기온별 의상 종류
    if temp >= 28:
        clothes = "민소매, 반바지, 린넨 소재 옷"
    elif 23 <= temp < 28:
        clothes = "반팔, 얇은 셔츠, 면바지"
    elif 17 <= temp < 23:
        clothes = "긴팔 티셔츠, 얇은 가디건, 슬랙스"
    elif 12 <= temp < 17:
        clothes = "자켓, 셔츠, 가디건, 청바지"
    elif 6 <= temp < 12:
        clothes = "코트, 가죽 자켓, 니트, 기모바지"
    else:
        clothes = "패딩, 두꺼운 코트, 목도리, 기모 제품"

    return color_palettes[color_type], clothes

def main():
    print("✨ 퍼스널 컬러 기반 내일의 코디 추천 프로그램 ✨")
    
    # 1. 퍼스널 컬러 진단
    my_color = get_personal_color()
    print(f"\n당신의 퍼스널 컬러는 [{my_color}]입니다!")

    # 2. 내일 날씨(기온) 입력
    try:
        tomorrow_temp = float(input("\n내일의 예상 낮 기온을 입력해주세요(°C): "))
        
        # 3. 결과 도출
        palette, clothes = get_outfit_recommendation(my_color, tomorrow_temp)
        
        print("\n" + "="*40)
        print(f"📅 [내일의 코디 제안]")
        print(f"추천 컬러: {palette}")
        print(f"추천 의상: {clothes}")
        print(f"포인트: {my_color}에 어울리는 컬러의 {clothes.split(',')[0]}를 입어보세요!")
        print("="*40)
    except ValueError:
        print("기온은 숫자로 입력해주세요.")

if __name__ == "__main__":
    main()
